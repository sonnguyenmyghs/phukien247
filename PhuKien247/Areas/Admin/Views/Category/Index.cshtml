@model List<Category>
@{
    ViewData["Title"] = "Quản lý danh mục";
}

@section Styles {
<style>
    .category-table th {
        font-size: 0.78rem;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        color: #6b7280;
        font-weight: 600;
        border-bottom: 2px solid #f3f4f6;
        padding: 14px 16px;
        white-space: nowrap;
    }
    .category-table td {
        vertical-align: middle;
        padding: 12px 16px;
        font-size: 0.88rem;
    }
    .category-table tbody tr {
        transition: background 0.15s;
    }
    .category-table tbody tr:nth-child(even) {
        background: #fafbfc;
    }
    .category-table tbody tr:hover {
        background: #fff7ed;
    }
    .cat-thumb {
        width: 48px;
        height: 48px;
        border-radius: 10px;
        object-fit: cover;
        border: 2px solid #f3f4f6;
    }
    .cat-thumb-placeholder {
        width: 48px;
        height: 48px;
        border-radius: 10px;
        background: #f3f4f6;
        display: flex;
        align-items: center;
        justify-content: center;
        color: #d1d5db;
    }
    .btn-action {
        width: 34px;
        height: 34px;
        border-radius: 8px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        font-size: 0.85rem;
        transition: all 0.2s;
        border: 1px solid;
        background: transparent;
        cursor: pointer;
    }
    .btn-action-edit { color: #f59e0b; border-color: #fde68a; }
    .btn-action-edit:hover { background: #f59e0b; color: #fff; border-color: #f59e0b; }
    .btn-action-delete { color: #ef4444; border-color: #fecaca; }
    .btn-action-delete:hover { background: #ef4444; color: #fff; border-color: #ef4444; }
</style>
}

<!-- Header -->
<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h5 class="fw-bold mb-1" style="color:#1f2937;">Quản lý danh mục</h5>
        <p class="text-muted mb-0" style="font-size:0.85rem;">@Model.Count danh mục trong he thong</p>
    </div>
    <a asp-area="Admin" asp-controller="Category" asp-action="Create"
       class="btn btn-primary d-flex align-items-center gap-2"
       style="background:#FF6B35;border-color:#FF6B35;border-radius:10px;padding:10px 20px;font-weight:600;">
        <i class="fas fa-plus"></i> Them danh mục
    </a>
</div>

<!-- Category Table -->
<div class="card" style="border-radius:14px;">
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table category-table mb-0">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Hình ảnh</th>
                        <th>Ten danh mục</th>
                        <th>Mô tả</th>
                        <th>So sản phẩm</th>
                        <th>Thao tac</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var category in Model)
                    {
                        <tr>
                            <td><strong class="text-muted">@category.Id</strong></td>
                            <td>
                                @if (!string.IsNullOrEmpty(category.ImageUrl))
                                {
                                    <img src="@category.ImageUrl" alt="@category.Name" class="cat-thumb"
                                         onerror="this.style.display='none';this.nextElementSibling.style.display='flex';" />
                                    <div class="cat-thumb-placeholder" style="display:none;">
                                        <i class="fas fa-image"></i>
                                    </div>
                                }
                                else
                                {
                                    <div class="cat-thumb-placeholder">
                                        <i class="fas fa-image"></i>
                                    </div>
                                }
                            </td>
                            <td><strong>@category.Name</strong></td>
                            <td class="text-muted">@(category.Description ?? "-")</td>
                            <td>
                                <span class="badge" style="background:#e0f2fe;color:#0284c7;border-radius:20px;padding:5px 12px;font-weight:600;">
                                    @category.Products.Count sản phẩm
                                </span>
                            </td>
                            <td>
                                <div class="d-flex gap-1">
                                    <a asp-area="Admin" asp-controller="Category" asp-action="Edit" asp-route-id="@category.Id"
                                       class="btn-action btn-action-edit" title="Chỉnh sửa">
                                        <i class="fas fa-pen"></i>
                                    </a>
                                    <form asp-area="Admin" asp-controller="Category" asp-action="Delete" asp-route-id="@category.Id"
                                          method="post" class="d-inline"
                                          onsubmit="return confirm('Ban co chac muon xoa danh mục nay?');">
                                        <button type="submit" class="btn-action btn-action-delete" title="Xóa">
                                            <i class="fas fa-trash-alt"></i>
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    }
                    @if (!Model.Any())
                    {
                        <tr>
                            <td colspan="6" class="text-center text-muted py-5">
                                <i class="fas fa-layer-group fa-2x mb-2 d-block" style="opacity:0.3;"></i>
                                Chưa có danh mục nao
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
